# 다음 진행 사항 (워크맵)

우선순위 순으로 정리. 완료되면 체크하고 [PROGRESS.md](./PROGRESS.md)에 날짜/내용을 남긴다.

## 1. 배포 마무리 (즉시)

- [ ] Worker 재배포: `cd worker && npx wrangler deploy` — 이번에 손본 토큰 절약 프롬프트(분량 제한, `max_tokens` 축소, 컨텍스트 축소)를 실제 서비스에 반영
- [ ] `worker/wrangler.toml`의 `ALLOWED_ORIGIN`을 `*`에서 `https://yuwonkyu.github.io`로 좁히기 (지금은 아무 사이트나 이 Worker를 가져다 쓸 수 있는 상태)
- [ ] 재배포 후 Anthropic 콘솔에서 실제 토큰/요금 사용량 확인 — 프롬프트 최적화 효과 검증

## 2. 플레이 검증

- [ ] Day 1~99 풀 플레이를 최소 1회 직접 진행 — 밸런스, 오탈자, 세계관 위반 여부 확인
- [ ] 3가지 종료 조건 각각 테스트: 체력 0(사망) / 99일 완주 / 플레이어 직접 포기
- [ ] 계승 시스템 실동작 확인: 한 캐릭터를 끝낸 뒤 새 캐릭터를 만들었을 때 이전 인물이 `legacyMentions`로 실제 언급되는지
- [ ] "복귀 요약 배너"(며칠 뒤 재접속 시나리오) 동작 확인
- [ ] Worker 응답 실패/네트워크 차단 상황에서 오프라인 폴백으로 자연스럽게 전환되는지 확인

## 3. 폴리싱

- [ ] 오프라인 폴백 생성기([offlineGenerator.ts](../../src/engine/offlineGenerator.ts)) 콘텐츠가 반복적으로 느껴지지 않는지 점검
- [ ] 좁은 뷰포트/실기기에서 최종 UI 재확인 (세로 9:16 레이아웃은 이미 적용됨)
- [ ] 로딩("이야기를 준비하는 중...")·에러 상태 UX 다듬기

## 4. 제출 준비

- [ ] [PROGRESS.md](./PROGRESS.md) 계속 갱신
- [ ] 데모용 스크린샷/짧은 플레이 영상 필요 여부 결정
- [ ] README "제출 자료" 섹션이 실제 제출 요건과 맞는지 최종 점검
