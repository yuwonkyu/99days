# 진행 상황

| 날짜 | 내용 |
|---|---|
| 2026-07-29 | 저장소 초기화(Expo+TS, web export 확인), 설계 문서(00~06) 작성, 인수인계 문서 기반 아키텍처 확정(Cloudflare Workers AI 프록시 + 오프라인 폴백) |
| 2026-07-30 | Expo+TS 스캐폴딩(타입/데이터/엔진/화면/컴포넌트), Cloudflare Worker AI 프록시 스캐폴드 구현 |
| 2026-07-31 | Cloudflare Worker KV 네임스페이스 id 연동, 웹 게임 UI를 모바일 세로(9:16) 컬럼으로 중앙 정렬 고정 |
| 2026-08-02 | GitHub Pages/Worker 배포 상태 점검(정상 동작 확인), README 플레이 링크 반영, AI 토큰 사용량 절감(프롬프트 분량 제한, `max_tokens` 축소, 컨텍스트 축소) |
| 2026-08-02 | 실플레이 중 15~21일 구간 상황 반복/정체 버그 발견 → `storyFlow.ts`(단계/외출·거처 흐름 유도), 오프라인 폴백 템플릿 확장(6→9, 거처형 보강), 응답 글자수 하드 캡(`textLimits.ts`)으로 대응 |
| 2026-08-02 | 오프라인 폴백을 조합형 콘텐츠 라이브러리로 재구축: 시드 36개(캐릭터당 ~152종 렌더), 성격/직업/체력/행운/단계/직전 로그 기반 가중 선택(`situationSelector.ts`), 선택지 lean→결과 확률→다음 상황 카테고리로 이어지는 확률적 흐름, 선택지 개수 2~4 가변화, 엔딩 3→57개(`endings.ts`/`endingSelector.ts`). 999개 목표는 장기 로드맵으로 이관 |
| 2026-08-02 | Worker 프롬프트/오프라인 라이브러리 배포, `ALLOWED_ORIGIN`을 GitHub Pages 도메인으로 제한, main 머지 및 GitHub Pages 자동 재배포 완료 |
| 2026-08-02 | 서사 흐름 피드백 반영: outcome=전날 요약→situation=자연스러운 오늘 도입부로 프롬프트 재정비, `storyDirective.timeSkip`(며칠~몇 년 시간 압축 서술, 12% 확률)과 `stat_changes.AGE` 추가, 오프라인 라이브러리에 `bond`(호감/우정/애정) 카테고리 신설(36→40 시드, ~184종 렌더). 배경 아트 키워드 프롬프트 로드맵을 [07-background-art-prompts.md](./07-background-art-prompts.md)로 별도 작성(설계만, 미구현) |
| 2026-08-02 | 스크린샷 피드백 반영: 오프라인 폴백이 "대가를 치러야 했다"처럼 결과를 뭉뚱그리던 것을 lean별 구체적 문구로 교체, 하루↔하루 사이에 아무 연결 문구 없이 시드가 점프하던 문제를 `buildTransitionPrefix()`("다음 날," / timeSkip 문구)로 완화 |
| 2026-08-03 | "결과가 어떻게 끝난 건지 모르겠다"는 후속 피드백 반영: 선택지를 우리 시드로 역추적해(`inferChoiceCategory`) 카테고리별 구체적 결말(`CATEGORY_RESOLUTIONS`)로 교체, 시드 자체 시간대 표현과 "다음 날,"이 겹치는 중복 제거. 스탯 변화(HP/STR/INT/AGI/LUK/AGE)를 `StatChangeBadges` 컴포넌트로 결과 옆에 시각화 |
| 2026-08-03 | "같은 상황이 반복해서 나온다" 피드백 반영: `GameState.usedSeedIds`로 오프라인 폴백이 이미 보여준 시드를 캐릭터별로 기억해 제외, 해당 모드(외출/거처)의 시드를 전부 소진하면 그때만 순환 리셋(`pickWeightedSeed`의 `wasReset`) |
| 2026-08-03 | 99일 완주 1판에 AI 호출이 최대 ~100회 필요함을 확인 — `DAILY_LIMIT`을 50→120으로 상향해 후반부도 AI로 이어지게 함, Worker 재배포. 이후 세션부터는 매 작업 끝에 확인 없이 바로 main 머지까지 진행하기로 함 |
| 2026-08-03 | 로드맵 2번(플레이 검증) 진행: 브라우저 자동화로 production/local 합쳐 60일 가까이 플레이하며 종료 조건 3종(사망/완주/포기), 계승 시스템(새 캐릭터 AI 응답에 이전 캐릭터가 실제 언급됨을 확인), 복귀 요약 배너, 오프라인 폴백 전환을 모두 검증 완료. 그 과정에서 **Worker가 200을 반환해도 `day_summary`(종종 `stat_changes`도)가 빠진 응답이 실측 40~50%에 달해 멀쩡한 AI 응답이 스키마 검증에서 거부되고 오프라인 폴백으로 낭비되던 버그**를 발견 — `aiClient.ts`의 검증을 `situation`/`choices`만 필수로 완화하고 나머지는 안전한 기본값으로 채우도록 수정(`max_tokens: 500` 트렁케이션이 유력 원인으로 추정, 근본 원인은 별도 확인 필요할 수 있음) |
| 2026-08-03 | 플레이 검증 중 situation 텍스트가 150자에서 단어 중간에 잘리는 버그 발견(사용자 스크린샷 제보: "이제 첫 손님을 기다"에서 끊김) — `textLimits.ts`의 `clamp()`가 "다"/"요" 글자가 마지막에 나온 위치를 무조건 문장 끝으로 오판해 생긴 문제. situation/outcome은 어차피 스크롤 가능한 패널 안에 있어 굳이 자를 UI적 이유가 없다고 판단해, 하드 캡을 없애고 대신 책장 넘기듯 "다음 ▶" 버튼으로 문장 단위(`paginate()`, 마침표 기준 분할이라 단어 중간 절단 없음)로 읽어나가는 UI를 새로 구축(`GameScreen.tsx`). 선택지는 마지막 페이지에서만 노출. choice/day_summary는 여전히 한 줄 UI라 캡을 유지하되, 자르는 로직을 단어 경계 기준으로 교체 |
| 2026-08-03 | "행운이 오르기만 한다"는 사용자 관찰 확인: 오프라인 폴백의 `STAT_DELTA_BY_TIER`가 `good`에서만 LUK+1을 주고 `bad`에서는 HP만 깎아 LUK가 절대 안 내려가는 구조였음(로컬 플레이 로그에서 실제로 48턴 만에 LUK가 상한 20까지 도달한 걸 확인). `bad` 결과에 LUK-1을 추가해 AI 경로(가끔 LUK 하락 있음)와 대칭 맞춤(`offlineGenerator.ts`) |
| 2026-08-03 | 배경 아트 로드맵(5번) 착수: 앱이 9:16 세로 고정(`App.tsx`)이라 배경도 9:16로 정함. offline 시드 40개를 `inferSceneTag()`로 직접 돌려본 결과 60%가 기본값 `city`로 빠짐을 확인(사교/코미디/호감류에 특정 키워드가 없고, `거처`가 `indoor` 키워드 목록에서 아예 빠져 있음)했고, 이를 근거로 SceneTag 6종(city/forest/market/danger/social/indoor) 전부에 대한 프롬프트를 작성. `BackgroundScene.tsx`를 이미지(그라디언트 위에 레이어) + 그라디언트 폴백 구조로 구현하고, `assets/backgrounds/`에 6장 모두 1x1 투명 PNG 자리표시자를 채워 지금 당장 빌드가 깨지지 않게 함 — 실제 아트를 같은 파일명으로 덮어쓰기만 하면 코드 변경 없이 적용됨 |
| 2026-08-03 | 배경 아트 실제 이미지(city/danger/forest/indoor/market) 적용 후, 배포 사이트에서 이미지가 화면의 좌상단 25%만 차지하는 버그 제보 → 원인은 react-native-web이 `require()`된 에셋의 실제 픽셀 크기(768×1376)를 `Image` 래퍼 div에 그대로 박아 넣는데 `StyleSheet.absoluteFill`은 inset만 지정하고 width/height는 건드리지 않아 CSS가 explicit size를 우선시하던 것 — `BackgroundScene.tsx`의 Image 스타일에 `width/height: '100%'`를 명시해 해결(PR #13, main 머지 완료, GitHub Pages 자동 재배포) |
| 2026-08-03 | "50~51일차 야생동물 조우 선택 직후 뜬금없이 예전에 헤어진 파수꾼과 창고에서 대치하는 이야기로 튐" 제보 — 원인은 AI에게 `recentDayLogs`(최근 3일 30자 요약)만 넘겨줘서 며칠 전 등장한 인물/사건을 모델이 기억하지 못하는 구조적 한계. `GameState.activeThread`(다일 스토리 스레드: 요약/카테고리/시작일/경과 턴)를 신설해 매 턴 명시적으로 왕복시키고, AI 응답에 `thread.status`(none/continuing/resolved)를 추가해 진행 중인 사안을 이어갈지 오늘 매듭지을지 모델이 답하게 함(`types/game.ts`, `promptBuilder.ts`, `worker/src/index.ts` TOOL_SCHEMA 동시 수정 — **Worker는 `cd worker && npx wrangler deploy`로 별도 재배포 필요**). 오프라인 폴백은 스레드를 이어 쓸 수 없으므로 조용히 무시하는 대신 "그 사이 …은 소강상태로" 식 한 줄로 항상 명시적으로 닫도록 처리(`offlineGenerator.ts`). 스레드가 열려 있는 동안은 `storyFlow.ts`의 `avoidRepeat`/`timeSkip`을 강제로 꺼서 다양성 유도 장치가 연속성과 충돌하지 않게 함. 별개로 "다음만 있고 이전이 없어 지난 내용을 다시 못 본다"는 피드백도 함께 반영해 같은 날 페이지 사이를 오갈 수 있는 "◀ 이전" 버튼 추가(`GameScreen.tsx`) |
| 2026-08-06 | 사용자 피드백 3건(홈 화면 부재, 사운드 없음, 생존율 체감상 너무 높음) 반영. ① `HomeScreen`/`RecordsScreen` 신설, `App.tsx`를 홈 허브 중심 상태 머신으로 재구성(새로 시작 시 진행 중 저장 있으면 확인 후 덮어씀, 삶이 끝나면 홈으로 복귀), `LegacyRecord`에 `job` 필드 추가. ② `expo-audio` 도입, `AudioProvider`(루프 재생/음소거 토글/영속 설정/브라우저 자동재생 정책 우회)와 `SoundToggleButton` 구현, `assets/audio/bgm.wav`에 자체 합성한 저작권 없는 placeholder 앰비언트 루프 삽입(실제 곡은 같은 파일명으로 교체하면 코드 변경 없이 적용). ③ 99일 중 초반/중반/후반 구간에 하나씩, 총 3일을 실제로 사망 가능한 "위기"로 배정(`storyFlow.generateCrisisDays`, `StoryDirective.crisisAhead`/`resolvingCrisis`) — 오프라인 폴백은 전용 위기 시드 5개(`situationSeeds.buildCrisisSeeds`)와 훨씬 가혹한 확률 분포/HP 손실(`offlineGenerator.ts`의 `CRISIS_*` 테이블, bad 시 HP -30대)로 구현, AI 경로는 `promptBuilder.ts`/`worker/src/index.ts` SYSTEM_PROMPT에 동일 안내 반영(**Worker는 별도로 `npx wrangler deploy` 필요**). 브라우저에서 실제로 위기 유발→사망→기록 반영까지 전 과정 검증 완료 |
| 2026-08-06 | 후속 피드백 4건 반영: 복귀 요약 배너가 30자 캡에 걸려 잘린 하루 요약 3개를 공백으로 이어붙여 "말하다 마는" 것처럼 읽히던 문제를 날짜별 불릿(`·`)+줄바꿈으로 분리(`gameStateStore.ts`). 스탯에 소수 변화(+0.3 등)가 누적되며 부동소수점 오차로 `11.60000000000001`처럼 표시되던 버그를 `applyStatDelta` 반올림 + `formatStat()` 표시 헬퍼로 해결(`statGen.ts`). 소지품이 직업 카테고리(5종) 단위로만 뭉뚱그려 나오던 것을 직업 23종 각각에 맞는 소지품으로 세분화(`characterGen.ts`). "상황이 비슷비슷 반복된다"는 피드백엔 직업군별 돌발 사건 뱅크(`JOB_INCIDENTS`)와 톤이 다른 신규 시드 6개(견제/뿌듯함/돌발 위험/코믹한 소동)를 추가해 오프라인 시드 풀을 40→46개로 확장(`situationSeeds.ts`) |

앞으로 진행 시 이 표에 날짜/내용을 추가한다. 다음 할 일은 [ROADMAP.md](./ROADMAP.md) 참고.
